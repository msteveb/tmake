fragment_downloaded_cb({"url":"debug-flags.html#build-goals","fragment":"Build goals\nThis option displays each goal (target) as it is examined in a hierarchical display.\nThis is useful as it shows the entire target dependency tree as it is traversed, not\njust the targets that need to be built.\n"});